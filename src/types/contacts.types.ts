export type Contact = {
  createdAt: string;
  name: string;
  email: string;
  phone: string;
  birthday: string;
  id: string;
};

export type NewContact = {
  createdAt: string;
  name: string;
  email: string;
  phone: string;
  birthday: string;
};
